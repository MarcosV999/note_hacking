- No uses `ifconfig` ➡️ Usa **`ip a`**
    
- No uses `netstat` ➡️ Usa **`ss`**
    
- No uses `arp` ➡️ Usa **`ip neigh`**
    
- No uses `nslookup` ➡️ Usa **`dig`** o **`host`**
    
- No uses `service` ➡️ Usa **`systemctl`**
    
- No uses `crackmapexec` ➡️ Usa **`netexec(nxc)`**
    
- No uses `enum4linux` ➡️ Usa **`enum4linux-ng`**